---
description: Start spec-driven development - write a structured specification before writing code (agent-skills spec-driven-development).
---

Invoke the `agent-skills:spec-driven-development` skill.

Turn the request into a structured spec (what is being built and why, with
acceptance criteria) before any implementation.

$ARGUMENTS
