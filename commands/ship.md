---
description: Run the pre-launch checklist via parallel specialist personas, then synthesize a go/no-go decision (agent-skills shipping-and-launch).
---

Invoke the `agent-skills:shipping-and-launch` skill.

Run the pre-launch checklist and return a synthesized go/no-go with the
blocking items called out.

$ARGUMENTS
